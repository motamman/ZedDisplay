Read `.claude/TODO.md` and display all items that are NOT marked as completed.

Show them grouped by section, preserving the original hierarchy. Completed items are those with `[x]` checkboxes — skip those entirely. Show items with `[ ]` or no checkbox.

Keep the output concise — just the open items, no commentary.
